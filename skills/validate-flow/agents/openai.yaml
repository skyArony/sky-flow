interface:
  display_name: "Validate Flow"
  short_description: "显式审计 Sky Flow artifact 结构与迁移残留"
  default_prompt: "使用 $validate-flow 对指定范围做 full-set 或 migration artifact audit。"
policy:
  allow_implicit_invocation: false
